<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.itheima.mapper.RstUserMapper">

    <select id="conditionQueryRstUser" resultType="com.itheima.pojo.RstUser">
        select id, username, age, gender, phone, password, avatar, level, signInCount from rstuser
        where
        username like concat('%',#{username},'%')
        and gender = #{gender}
        and phone = #{phone}
    </select>
    <select id="conditionQueryRstUserGenderIsNull" resultType="com.itheima.pojo.RstUser">
        select id, username, age, gender, phone, password, avatar, level, signInCount from rstuser
        where
            username like concat('%',#{username},'%')
          and phone = #{phone}
    </select>
    <select id="conditionQueryRstUserPhoneIsNull" resultType="com.itheima.pojo.RstUser">
        select id, username, age, gender, phone, password, avatar, level, signInCount from rstuser
        where
            username like concat('%',#{username},'%')
          and gender = #{gender}
    </select>
    <select id="conditionQueryRstUserGenderAndPhoneAreNull" resultType="com.itheima.pojo.RstUser">
        select id, username, age, gender, phone, password, avatar, level, signInCount from rstuser
        where
            username like concat('%',#{username},'%')
    </select>
    <select id="queryRstUserByRstUserName" resultType="com.itheima.pojo.RstUser">
        select id, username, age, gender, phone, password, avatar, level, signInCount from rstuser
        where username = #{userName}
    </select>
    <select id="dynamicSqlTest" resultType="com.itheima.pojo.RstUser">
        select id, username, age, gender, phone, password, avatar, level, signInCount from rstuser
        <where>
            <if test="username != null and username != ''">
                username like concat('%',#{username},'%')
            </if>
            <if test="gender != null">
                and gender = #{gender}
            </if>
            <if test="age != null">
                and age = #{age}
            </if>
        <if test="phone != null">
                and phone = #{phone}
        </if>
        </where>
    </select>
</mapper>